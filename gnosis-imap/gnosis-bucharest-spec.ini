[addOwnerWithThreshold-success] 
statusCode: _ => EVMC_SUCCESS 
output: _ => _ 
callData: #abiCallData("addOwnerWithThreshold", #address(OWNER), #uint256(NEW_THRESHOLD)) 
log: _:List ( .List => ListItem(#abiEventLog(#PROXY_ID, "AddedOwner", #indexed(#address(OWNER)))) ) 
this: PROXY_ID 
storage: M1:IMap => M2:IMap
requires:
ensures:
    ensures
    select(M1, #hashedLocation({COMPILER}, {OWNERS}, OWNER)) ==Int SENTINEL_OWNERS
    select(M1, )
